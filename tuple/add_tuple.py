#instead of append and extend, use add(concatination)
original_tuple = (1, 2, 3)
new_elements = (4, 5)

# Create a new tuple by concatenating the original tuple and the new elements
new_tuple = original_tuple + new_elements

print(new_tuple)  # Output: (1, 2, 3, 4, 5)